import { ImageScaleDirective } from './image-scale.directive';

describe('ImageScaleDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageScaleDirective();
    expect(directive).toBeTruthy();
  });
});
